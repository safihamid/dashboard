%h3 Recent drawings that students have saved to their gallery
%table
  %tr
    - @activities.each do |activity|
      %tr
        %td
          = link_to image_tag(original_image_level_source_path(activity.level_source)), activity.level_source
        %td
          by
          = activity.user.name
          = time_ago_in_words activity.created_at
          ago
